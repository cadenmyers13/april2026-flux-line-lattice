# April 2026 Flux Line Lattice Experiment at ORNL

This is the codebase of the FLL experiment.

To run the `scripts/saf_analysis.py` you must install `saf-classifier` into your env.
Do this by running from the top level dir,

```
pip install saf-classifier
```
